version: "3.8"

services:
  prepilot-backend:
    build: .
    container_name: prepilot-backend
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    restart: unless-stopped
    # Uncomment below if you need
